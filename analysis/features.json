{
  "_doc": "Feature registry for the player-similarity engine. Each feature belongs to a GROUP; the group's weight is split evenly among its features, so adding a correlated feature dilutes within its group instead of inflating that concept's total weight. To add a new data source: add a feature row here (name must match a key in the merged per-player record). 'log' applies log1p before robust-scaling (for skewed counts). 'cap' bounds each per-feature distance: differences beyond `cap` robust-IQRs count as maximally different (keeps the metric bounded so a new feature can never blow up the scale). Path/consistency features come from analysis/path_features.py (player_participation.json); they distinguish teammates by career PATH and placement depth, not just shared trophies.",
  "cap": 4.0,
  "_note_era": "EVERY scored feature must be era-independent: an era-adjusted value (adjAll, peakAll), a rate (win_conversion, finals/deep-run), a year count (spanAll, tenure_years), or an era-neutral count (champs, titles, teams). Deliberately EXCLUDED as era-tainted: `raw` (early seasons had 17 majors vs 6-7 now, and it 0.90-duplicates adjAll), `attendances` (calendar density, not career), and majors-based tenure (replaced by tenure_years = spanAll/distinct_teams). Debut year is shown only as non-scored context.",
  "groups": {
    "success": {
      "weight": 0.42,
      "features": {
        "adjAll":         { "log": true,  "label": "Era-adjusted wins" },
        "champs":         { "log": true,  "label": "World Championships" },
        "win_conversion": { "log": false, "label": "Win conversion" }
      }
    },
    "peak": {
      "weight": 0.18,
      "features": {
        "peakAll": { "log": false, "label": "Peak season value" }
      }
    },
    "placement": {
      "weight": 0.10,
      "_note": "Demoted: rates are opportunity-normalized but NOT field-size-normalized (top-4 in an 8-team playoff != top-4 in a 32-team open), so low weight.",
      "features": {
        "finals_rate":   { "log": false, "label": "Finals rate (top-2)" },
        "deep_run_rate": { "log": false, "label": "Deep-run rate (top-4)" }
      }
    },
    "longevity": {
      "weight": 0.14,
      "features": {
        "titlesAll": { "log": false, "label": "Distinct titles won" },
        "spanAll":   { "log": false, "label": "Career span (yrs)" }
      }
    },
    "path": {
      "weight": 0.16,
      "features": {
        "distinct_teams": { "log": true,  "label": "Teams played for" },
        "tenure_years":   { "log": false, "label": "Avg tenure (yrs/team)" }
      }
    }
  }
}
